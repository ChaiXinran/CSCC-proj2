//! `Object` constructor and prototype bootstrap.

use crate::runtime::Heap;

pub fn install_object(_heap: &mut Heap) {
    // Constructor and prototype wiring lands with the object-model milestone.
}
