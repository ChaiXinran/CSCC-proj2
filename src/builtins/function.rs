//! `Function` constructor and prototype bootstrap.

use crate::runtime::Heap;

pub fn install_function(_heap: &mut Heap) {
    // Callable objects land with VM call frames and lexical environments.
}
