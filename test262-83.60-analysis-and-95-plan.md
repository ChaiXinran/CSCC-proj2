# AgentJS 最新 Test262 全量测试分析与 95% 冲刺计划

> 仓库：`ChaiXinran/CSCC-proj2`  
> 基线提交：当前 `main`（分析时最新提交为 `0571a6b1a4b33ef8ad5d5f24f21315d0f7d5e8f4`）  
> 日志：`detail_latest.txt`  
> 测试命令：`cargo run --release -- test262 --jobs 4 --progress --json reports/full-test262-summary.json`

---

## 一、执行摘要

本轮 Test262 全量测试共执行 **53,379** 个测试实例：

| 指标 | 最新结果 |
|---|---:|
| 通过 | **44,626** |
| 失败 | **8,751** |
| 跳过 | **2** |
| 规范一致性 | **83.60%** |
| 测试耗时 | **706.91 s** |
| 平均吞吐量 | **75.51 tests/s** |
| Release 编译耗时 | 55.08 s |

与上一轮 76.54% 基线相比：

| 指标 | 上一轮 | 最新一轮 | 变化 |
|---|---:|---:|---:|
| 通过 | 40,854 | **44,626** | **+3,772** |
| 失败 | 12,523 | **8,751** | **−3,772** |
| 跳过 | 2 | 2 | 0 |
| 通过率 | 76.54% | **83.60%** | **+7.06 pp** |

本轮是一次非常显著的功能性提升，且不是通过扩大 skip 范围得到的：测试总数与跳过数保持不变，净新增通过 3,772 项。

不过，全量运行耗时从上一轮的 293.84 秒增加到 706.91 秒。由于两轮日志的代码版本、机器路径和运行环境不完全一致，不能直接断言引擎性能退化；但进入 90% 以后，应把全量耗时和超时项纳入回归门禁，避免通过增加昂贵兼容路径换取分数。

---

## 二、距离 95% 还有多远

95% 的最低通过数量为：

```text
ceil(53,379 × 0.95) = 50,711
```

因此还需要：

```text
50,711 − 44,626 = 6,085
```

也就是说，需要把当前 8,751 个失败中的至少 **6,085 个转为通过**，净消除约 **69.53%** 的现有失败。

当两个 skip 保持不变时，95% 对应：

| 项目 | 数量 |
|---|---:|
| 最低通过数 | 50,711 |
| 允许的非通过总数 | 2,668 |
| 其中固定 skip | 2 |
| 最多允许失败 | **2,666** |
| 当前失败 | 8,751 |
| 必须净减少 | **6,085** |

这意味着 95% 已经不能依靠“补若干缺失方法”完成，而必须系统修复一批共享语义和大型标准库族。

### 里程碑

| 目标 | 需要通过数 | 相比当前净增 |
|---:|---:|---:|
| 85% | 45,373 | +747 |
| 88% | 46,974 | +2,348 |
| 90% | 48,042 | +3,416 |
| 92% | 49,109 | +4,483 |
| 93% | 49,643 | +5,017 |
| 94% | 50,177 | +5,551 |
| **95%** | **50,711** | **+6,085** |

建议把 90%、93%、95% 设为三个正式验收点，而不是只在最后跑一次全量测试。

---

## 三、失败分布

### 3.1 一级目录

| Test262 分区 | 最新失败 | 占全部失败 | 上一轮失败 | 变化 |
|---|---:|---:|---:|---:|
| `built-ins` | 2,769 | 31.64% | 4,728 | **−1,959** |
| `language` | 2,734 | 31.24% | 4,026 | **−1,292** |
| `intl402` | 2,279 | 26.04% | 2,756 | **−477** |
| `staging` | 597 | 6.82% | 665 | −68 |
| `annexB` | 359 | 4.10% | 335 | **+24** |
| `harness` | 13 | 0.15% | 13 | 0 |
| **合计** | **8,751** | **100%** | **12,523** | **−3,772** |

主要进步来自：

1. `built-ins`：减少 1,959；
2. `language`：减少 1,292；
3. `intl402`：减少 477。

`annexB` 反而增加 24，是当前唯一明确扩大的一级失败区，应列入回归修复任务。

### 3.2 当前最大的二级失败簇

| 排名 | 功能簇 | 失败数 | 占全部失败 |
|---:|---|---:|---:|
| 1 | `intl402/Temporal` | **1,326** | 15.15% |
| 2 | `language/expressions` | **1,030** | 11.77% |
| 3 | `built-ins/Temporal` | **921** | 10.52% |
| 4 | `language/statements` | **876** | 10.01% |
| 5 | `staging/sm` | 556 | 6.35% |
| 6 | `built-ins/RegExp` | 416 | 4.75% |
| 7 | `annexB/language` | 337 | 3.85% |
| 8 | `built-ins/Object` | 248 | 2.83% |
| 9 | `language/eval-code` | 227 | 2.59% |
| 10 | `intl402/NumberFormat` | 200 | 2.29% |
| 11 | `language/module-code` | 194 | 2.22% |
| 12 | `intl402/DateTimeFormat` | 173 | 1.98% |
| 13 | `built-ins/Array` | 153 | 1.75% |
| 14 | `built-ins/Atomics` | 118 | 1.35% |
| 15 | `intl402/Locale` | 116 | 1.33% |
| 16 | `language/import` | 116 | 1.33% |
| 17 | `intl402/DurationFormat` | 110 | 1.26% |
| 18 | `built-ins/Function` | 80 | 0.91% |
| 19 | `intl402/Segmenter` | 77 | 0.88% |
| 20 | `language/arguments-object` | 71 | 0.81% |

前四个功能簇合计 4,153 个失败，占所有失败的 **47.46%**。因此，Temporal、表达式和语句语义仍然是冲击 95% 的主战场。

---

## 四、本轮主要进展

| 功能簇 | 上一轮失败 | 最新失败 | 减少 |
|---|---:|---:|---:|
| `language/expressions` | 1,833 | 1,030 | **−803** |
| `built-ins/Temporal` | 1,689 | 921 | **−768** |
| `intl402/Temporal` | 1,799 | 1,326 | **−473** |
| `language/statements` | 1,333 | 876 | **−457** |
| `built-ins/Atomics` | 388 | 118 | **−270** |
| `built-ins/TypedArray` | 188 | 19 | **−169** |
| `built-ins/Promise` | 186 | 24 | **−162** |
| `built-ins/Array` | 301 | 153 | **−148** |
| `built-ins/Iterator` | 148 | 17 | **−131** |
| `language/module-code` | 245 | 194 | −51 |
| `built-ins/RegExp` | 432 | 416 | −16 |
| `annexB/language` | 313 | 337 | **+24** |

### 结论

- 上一轮的 Iterator 回归已经基本修复，从 148 降到 17。
- TypedArray、Promise、Atomics、Array 都取得大幅进展。
- Temporal 仍然在快速改善，但剩余量依然巨大。
- RegExp 几乎没有动，说明当前后端能力已经成为结构性瓶颈。
- Annex B 的作用域与声明实例化问题有所扩大。

---

## 五、剩余失败不是 8,751 个独立小问题

日志中的大量失败具有明显的“一个根因影响几十到几百项测试”的特征。

### 5.1 Temporal / Intl Temporal：2,247 项

主要子簇：

| 子簇 | 失败 |
|---|---:|
| `intl402/Temporal/ZonedDateTime` | 506 |
| `intl402/Temporal/PlainDateTime` | 283 |
| `intl402/Temporal/PlainDate` | 270 |
| `built-ins/Temporal/ZonedDateTime` | 190 |
| `built-ins/Temporal/Duration` | 174 |
| `intl402/Temporal/PlainYearMonth` | 147 |
| `built-ins/Temporal/PlainDate` | 145 |
| `built-ins/Temporal/PlainDateTime` | 140 |
| `built-ins/Temporal/PlainYearMonth` | 126 |

重复错误包括：

- `invalid Temporal monthCode`；
- `invalid Temporal.PlainDate`；
- options/getter 访问顺序错误；
- `RangeError` 与 `TypeError` 时机错误；
- `ZonedDateTime` 时区/偏移/格式化；
- `PlainDate*`、`Duration` 的 balance、round、add/subtract；
- Intl 格式化与 Temporal 类型桥接。

这里应由一个人统一维护日期、时间、历法、时区、rounding 和格式化协议，不能再把 PlainDate、Duration、ZonedDateTime 分给不同成员，否则共享 ISO 算法和 options 解析会反复冲突。

### 5.2 Language pipeline：表达式、语句、class、eval

主要子簇：

| 子簇 | 失败 |
|---|---:|
| `language/statements/class` | 245 |
| `language/expressions/class` | 203 |
| 其他 class/staging class | 约 52 |
| `language/eval-code` | 227 |
| `annexB/language/eval-code` | 201 |
| `annexB/language/global-code` | 81 |
| `language/statements/try` | 118 |
| `language/expressions/compound-assignment` | 111 |
| `language/expressions/object` | 105 |
| `language/statements/for` | 64 |
| `language/statements/await-using` | 66 |

典型根因：

- parser/early-error 不完整；
- class element 初始化与 private brand；
- `super()`、`this`、derived constructor；
- destructuring 中 abrupt completion 与 iterator close；
- `try/catch/finally` completion 覆盖规则；
- eval/global declaration instantiation；
- Annex B sloppy block function；
- computed property key 和 getter/callback 调用顺序；
- async/generator completion。

这些问题横跨 AST、Parser、Bytecode、VM、Environment，必须由一个 feature owner 端到端负责，而不是按 parser、compiler、runtime 三段分给三个人。

### 5.3 模块与异步协议

相关失败至少包括：

- dynamic import：137；
- module-code：194；
- import/import-defer：116；
- top-level await；
- Promise/async completion；
- async job drain；
- namespace、live binding、TDZ。

这里的核心不是某个 `import()` 方法，而是：

1. ModuleRecord 状态机；
2. linking/evaluation；
3. namespace object；
4. Promise capability；
5. job queue；
6. top-level await；
7. 失败的异步 reject 时机。

必须由同一个模块/异步 owner 维护。

### 5.4 对象模型与共享抽象操作

当前仍有大量错误表现为：

- `undefined is not callable`；
- `undefined is not a constructor`；
- getter 必须走 VM call path；
- property descriptor 不匹配；
- 应抛 TypeError/RangeError 但未抛；
- species constructor；
- IteratorClose；
- Realm 与 prototype；
- callback abrupt completion；
- `SameValue` / `SameValueZero`；
- `ToPrimitive` / `ToPropertyKey` / `ToLength`。

这些错误会同时影响 Array、Object、Function、Map/Set、Iterator、TypedArray、Temporal、Intl。继续在各 builtin 文件中各写一套 helper，会产生新的行为差异。

### 5.5 RegExp：487 项相关失败

日志反复出现：

- backreference 不支持；
- lookaround 不支持；
- Unicode sets；
- Unicode property escapes；
- regexp modifiers；
- named groups；
- Annex B legacy escapes；
- `RegExp.prototype.compile` brand check；
- match indices；
- 错误类型和解析阶段不一致。

当前实现使用的通用 Rust regex 后端无法天然覆盖完整 ECMAScript RegExp。要到 95%，RegExp 不能继续只做外围修补，必须做一次后端/兼容层架构决策。

### 5.6 Atomics 与 harness

Atomics 失败已从 388 降到 118，是明显进展。但仍有 112 项典型错误来自：

```text
harness atomicsHelper.js failed:
Cannot read properties of undefined (reading 'getReport')
```

这更像 Test262 Agent harness/报告协议未接通，而不一定是 112 个独立 Atomics 算法错误。

两个 skip 仍为 non-blocking agent 测试未启用，不应算作功能通过。

---

## 六、95% 是否可行

### 判断

**可行，但需要把下一版本当作架构整合版本，而不是继续做零散 score patch。**

当前 8,751 个失败中，路径归并后可形成三个相对完整的所有权池：

| 工作池 | 当前失败池 | 说明 |
|---|---:|---|
| A：语言语义流水线 | 约 **2,799** | Parser、Class、Eval、Annex B、Control Flow |
| B：运行时协议与对象/异步/模块 | 约 **2,062** | Abstract Ops、Object、Iterator、Promise、Module |
| C：专用标准库引擎 | 约 **3,890** | Temporal、Intl402、RegExp、Binary、Atomics |
| **总计** | **8,751** | 无重叠路径归属 |

为留出回归余量，不能只设定 +6,085 的毛目标。建议设置：

| 组 | 毛增益目标 | 当前池占比 |
|---|---:|---:|
| A | **+2,100** | 75.0% |
| B | **+1,500** | 72.7% |
| C | **+2,900** | 74.6% |
| **总毛目标** | **+6,500** | 74.3% |

若实现 +6,500，即使整合产生约 400 个回归，仍有机会保持 95%。

这非常有挑战，但比把每组任务写成几十个互相交叉的小点更可执行。

---

# 七、建议的三人分工

以下按项目现有 A/B/C 三人并行模式设计。

## A 组：语言语义流水线 Owner

### 端到端所有权

```text
Lexer / Parser
    -> AST
    -> Bytecode lowering
    -> VM completion
    -> Environment / private fields
```

### 主要文件

```text
src/ast/
src/lexer*
src/parser/
src/bytecode/compiler.rs
src/bytecode/opcode.rs
src/vm/interpreter.rs              # 语言 opcode 与 completion 部分
src/runtime/environment.rs
src/runtime/private.rs
```

### 独占功能

1. class 全部语义；
2. eval 与 Annex B language；
3. scope、declaration instantiation、TDZ；
4. destructuring；
5. try/catch/finally completion；
6. for/for-in/for-of；
7. async/generator 的语法、lowering 和 VM completion；
8. parse-phase 与 early-error；
9. object literal、compound assignment、private name；
10. staging 中对应的 language/parser 测试。

### 不应再拆分

- class 不能拆成 parser、private field、static block 三个人；
- eval 与 Annex B global/function declaration 必须同一人；
- destructuring 与 IteratorClose 的语言侧语义由 A 统一提出调用需求，B 提供共享操作。

### 目标

```text
当前池：约 2,799
毛增益目标：+2,100
保留失败预算：约 699
```

### 第一优先级

1. class element plan 与初始化顺序；
2. eval/global declaration instantiation；
3. Annex B block function；
4. try/finally completion；
5. destructuring iterator close；
6. early-error 集中修复。

---

## B 组：运行时协议、对象模型、异步与模块 Owner

### 端到端所有权

```text
Abstract Operations
    -> Object / Property / Function / Realm
    -> Iterator
    -> Promise / Job Queue
    -> Module Registry / Dynamic Import
    -> Builtin shared semantics
```

### 主要文件

```text
src/runtime/context.rs
src/runtime/coercion.rs
src/runtime/object.rs
src/runtime/property.rs
src/runtime/property_map.rs
src/runtime/value.rs
src/runtime/function.rs
src/runtime/iterator.rs
src/runtime/job.rs
src/runtime/module.rs
src/runtime/symbol.rs

src/builtins/object.rs
src/builtins/array.rs
src/builtins/function.rs
src/builtins/collections.rs
src/builtins/promise.rs
src/builtins/proxy.rs
src/builtins/error.rs
src/builtins/string.rs
src/builtins/mod.rs
```

### 独占功能

1. `ToPrimitive`、`ToNumeric`、`ToString`、`ToPropertyKey`；
2. `Get`、`Set`、`HasProperty`、`GetMethod`；
3. Call/Construct；
4. `OrdinaryCreateFromConstructor`、Realm Intrinsics；
5. SpeciesConstructor；
6. Iterator protocol 与 IteratorClose；
7. Promise capability、reaction、job queue；
8. module linking/evaluation；
9. dynamic import；
10. namespace object、live binding、TLA；
11. WeakRef、FinalizationRegistry、ShadowRealm 的运行时记录；
12. Array/Object/Function/Map/Set 等依赖共享协议的 builtins。

### 不应再拆分

- dynamic import 从 parse 到 Promise reject 必须由 B 统一负责；
- Promise 与 job queue 不分给不同人；
- Iterator helper 与 Array.from/TypedArray.from 的 iterator 协议只允许 B 提供一套；
- Object/Property/Realm 由 B 独占，其他组只调用接口。

### 目标

```text
当前池：约 2,062
毛增益目标：+1,500
保留失败预算：约 562
```

### 第一优先级

1. 建立统一 Abstract Operations；
2. 消除 accessor/callback 不走 VM call path；
3. 完成 module/dynamic import/TLA 状态机；
4. 修复 species/iterator close；
5. ShadowRealm、WeakRef、FinalizationRegistry 的全局与内部槽；
6. Object/Function descriptor 与 Realm。

---

## C 组：Temporal / Intl / RegExp / Binary 专用引擎 Owner

### 端到端所有权

```text
Temporal + Intl algorithms
RegExp parsing/execution compatibility
ArrayBuffer/TypedArray/Atomics substrate
```

### 主要文件

```text
src/builtins/date_intl.rs
src/builtins/regexp.rs
src/builtins/annex_b.rs            # RegExp Annex B
src/builtins/binary_data.rs
src/runtime/buffer.rs
src/runtime/bigint.rs
```

建议把 `date_intl.rs` 内部拆成代码模块，但不要拆成多人所有权：

```text
src/builtins/temporal/
    iso.rs
    options.rs
    duration.rs
    plain_date.rs
    plain_time.rs
    plain_date_time.rs
    year_month.rs
    month_day.rs
    zoned_date_time.rs

src/builtins/intl/
    locale.rs
    number_format.rs
    date_time_format.rs
    duration_format.rs
    temporal_bridge.rs
```

### 独占功能

1. 所有 Temporal 类型与共享 ISO 算法；
2. Temporal options、rounding、calendar、time-zone；
3. Intl402 全部格式化器；
4. Temporal ↔ Intl bridge；
5. RegExp grammar/compile/exec；
6. ArrayBuffer、SharedArrayBuffer、DataView、TypedArray；
7. Atomics 和 Test262 agent harness 接口；
8. BigInt binary storage。

### 不应再拆分

- PlainDate、Duration、ZonedDateTime 不能交给不同人；
- NumberFormat/DateTimeFormat 与 Temporal bridge 同一 owner；
- RegExp parser 与 exec backend 同一 owner；
- Atomics 与 SharedArrayBuffer/harness 同一 owner。

### 目标

```text
当前池：约 3,890
毛增益目标：+2,900
保留失败预算：约 990
```

### 第一优先级

1. Temporal shared ISO/options/rounding；
2. Intl Temporal bridge；
3. NumberFormat/DateTimeFormat 高频路径；
4. RegExp backend 决策并一次性迁移；
5. Atomics harness `getReport`；
6. binary-data 边界行为。

---

# 八、共享协议草案

## 8.1 新增统一 Abstract Operations 层

建议新增：

```text
src/runtime/abstract_ops.rs
```

由 B 组独占维护。其他 builtin 不再复制本地 helper。

第一批冻结接口：

```rust
pub fn get(vm: &mut Vm, ctx: &mut NativeContext, value: JsValue, key: PropertyKey)
    -> Result<JsValue, VmError>;

pub fn get_method(vm: &mut Vm, ctx: &mut NativeContext, value: JsValue, key: PropertyKey)
    -> Result<Option<JsValue>, VmError>;

pub fn call(vm: &mut Vm, ctx: &mut NativeContext, callable: JsValue, this_value: JsValue,
    args: &[JsValue]) -> Result<JsValue, VmError>;

pub fn construct(vm: &mut Vm, ctx: &mut NativeContext, constructor: JsValue,
    args: &[JsValue], new_target: JsValue) -> Result<JsValue, VmError>;

pub fn to_primitive(vm: &mut Vm, ctx: &mut NativeContext, value: JsValue,
    preferred: PreferredType) -> Result<JsValue, VmError>;

pub fn to_integer_or_infinity(...);
pub fn to_length(...);
pub fn to_index(...);
pub fn to_property_key(...);

pub fn ordinary_create_from_constructor(...);
pub fn species_constructor(...);
pub fn array_species_create(...);

pub fn get_iterator(...);
pub fn iterator_next(...);
pub fn iterator_complete(...);
pub fn iterator_value(...);
pub fn iterator_close(...);

pub fn new_promise_capability(...);
pub fn perform_promise_then(...);
```

具体签名可根据现有类型调整，但语义只能有一份。

## 8.2 VM 调用协议

任何可能触发 JavaScript 用户代码的操作必须走 VM：

- getter/setter；
- Proxy trap；
- callback；
- comparator；
- iterator method；
- `Symbol.toPrimitive`；
- species constructor；
- Temporal/Intl options getter。

禁止在 builtin 中直接读取数据属性并假定没有 accessor。

## 8.3 Completion 协议

A 与 B 统一使用现有 `Completion` / `VmError` 语义：

- parse/early error 只在 parser/static-semantics 阶段产生；
- runtime TypeError/RangeError 不得伪装成 compile unsupported；
- `return`、`break`、`continue`、`throw`、normal completion 明确区分；
- finally 的 completion 覆盖遵循统一函数；
- IteratorClose 接收原始 abrupt completion 并按规范决定返回哪个异常。

## 8.4 Internal Slots 协议

新增复杂对象优先使用：

```text
ObjectKind + typed runtime record
```

不要继续新增可通过普通属性路径观察到的：

```text
__agentjs_*__
```

内部字段。

特别适用于：

- Iterator Helper；
- Promise；
- Temporal；
- WeakRef / FinalizationRegistry；
- ShadowRealm；
- Module namespace；
- DisposableStack；
- RegExp match state。

## 8.5 Realm / Intrinsics 协议

- constructor/prototype 从当前 Realm 的 `Intrinsics` 获取；
- 不得从 global object 硬编码读取默认构造器；
- cross-Realm brand check、prototype 和 error constructor 必须一致；
- 测试需要“同名但不同 Realm 的 Error constructor”时不能只比较名字。

## 8.6 Async / Job Queue 协议

- 所有 Promise reaction 和 async continuation进入统一 `JobQueue`；
- builtin 不得自行 drain jobs；
- 顶层 runner、module evaluation、Test262 async harness 使用统一 drain 边界；
- dynamic import、TLA、async iterator、AsyncDisposableStack 共用该协议；
- job 失败只能通过 reject/throw 传播，不得变成“async test did not signal completion”。

## 8.7 Feature Owner 跨层规则

“一个功能只归一个 owner”不等于“一个人只能修改固定目录”。

例如 dynamic import 归 B：

- B 可以修改 parser/AST/compiler 中 dynamic-import 相关代码；
- 但必须在 A 的 shared-file lock 中登记；
- A 不同时修改同一函数；
- B 对该功能从语法到运行结果负责，不把失败推给 A。

同理，Temporal 归 C：

- 若需要 `ToPrimitive`，C 不自己实现；
- C 提交 Abstract Ops 接口需求；
- B 合并接口后，C 只调用统一接口。

---

# 九、冲突控制与合并策略

## 9.1 Shared-file lock

建立：

```text
docs/version/native-v16-locks.md
```

每个共享文件记录：

| 字段 | 含义 |
|---|---|
| file/function | 文件与函数范围 |
| owner | 当前修改者 |
| feature | 服务的完整功能 |
| interface impact | 是否改共享接口 |
| start commit | 开始 SHA |
| expected merge | 合并顺序 |
| released | 是否释放锁 |

重点锁文件：

```text
src/builtins/mod.rs
src/runtime/context.rs
src/runtime/object.rs
src/vm/interpreter.rs
src/bytecode/compiler.rs
src/parser/expression.rs
src/parser/statement.rs
```

## 9.2 推荐分支

```text
docs/v16-95-contracts
feat/v16-a-language-semantics
feat/v16-b-runtime-protocols
feat/v16-c-spec-engines
integration/v16-95
```

## 9.3 推荐合并顺序

```text
共享协议与 baseline manifest
    -> B Abstract Operations v1
    -> A class/eval/completion 第一批
    -> C Temporal shared core 第一批
    -> B iterator/species/object 第一批
    -> A destructuring/try/early-error
    -> C Intl/RegExp/Atomics 第一批
    -> B module/dynamic import/TLA
    -> 全量测试
    -> 回归修复
    -> 第二轮大簇冲刺
    -> 95% 全量验收
```

不要让三组长期在各自分支积累几千行后一次性合并。

---

# 十、执行阶段

## Phase 0：冻结基线与协议

产物：

```text
reports/full-test262-summary-v16-baseline.json
reports/full-test262-failures-v16-baseline.txt
docs/version/native-v16-interface.md
docs/version/native-v16-team-plan.md
docs/version/native-v16-locks.md
```

必须记录：

- Git commit SHA；
- Test262 revision；
- Rust version；
- OS/CPU；
- jobs；
- timeout/environment variables；
- pass/fail/skip；
- 每组 focused JSON。

## Phase 1：先到 88%

目标：+2,348。

- A：class/eval/early-error 第一批；
- B：Abstract Ops、VM accessor/callback、species/iterator；
- C：Temporal shared ISO/options、Atomics harness。

验收：

```text
>= 46,974 passed
无目录级大回归
全量失败清单可重现
```

## Phase 2：到 90%

目标：累计 +3,416。

- A：try/finally、destructuring、Annex B；
- B：module/dynamic import/Promise job；
- C：Intl Temporal、NumberFormat/DateTimeFormat 高频路径。

验收：

```text
>= 48,042 passed
RuntimeLimit 不明显增加
async did-not-signal 显著下降
```

## Phase 3：到 93%

目标：累计 +5,017。

- A：剩余 language/staging semantic clusters；
- B：Realm、ShadowRealm、WeakRef、Object/Function；
- C：RegExp backend、Temporal ZonedDateTime、Intl formatting。

验收：

```text
>= 49,643 passed
RegExp 失败明显下降
Temporal + Intl Temporal 剩余 < 1,000
```

## Phase 4：到 95%

目标：累计 +6,085，建议毛增益 +6,500。

- 根据全量差异只修剩余大簇；
- 禁止为了分数扩大 skip；
- 清理回归；
- 固化最终报告与 README。

验收：

```text
passed >= 50,711
failed <= 2,666（skip 仍为 2 时）
conformance >= 95.00%
```

---

# 十一、每个 PR 的强制门禁

每个功能 PR 必须包含：

1. 失败根因说明；
2. 共享协议是否变化；
3. focused baseline JSON；
4. focused after JSON；
5. 新增通过数；
6. 新增失败数；
7. 至少一个原生单元测试；
8. 相关 Test262 目录测试；
9. 5,000-case regression manifest；
10. 格式、clippy、unit test。

建议命令：

```sh
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test --all-targets

cargo run --release -- test262 \
  --jobs 4 \
  --progress \
  --json reports/full-test262-summary.json
```

每次 full run 后自动生成：

```text
new_passes.txt
new_failures.txt
unchanged_failures.txt
failure-count-by-directory.csv
failure-count-by-signature.csv
```

门禁规则：

```text
net pass delta >= 0
重点目录不得无说明回归
skip 不得增加
RuntimeLimit 不得大幅增加
full summary 必须与日志尾部一致
```

---

# 十二、近期最值得立刻做的五件事

1. **先合并 Abstract Operations v1**，停止在 builtin 中继续复制 `is_callable`、iterator、species、coercion 等 helper。
2. **A 组统一处理 class + eval + Annex B**，不要再按语法点拆分。
3. **C 组重构 Temporal shared core**，优先统一 options、ISO balance、monthCode、rounding、ZonedDateTime。
4. **对 RegExp 做一次后端决策**，不要继续只修外围属性。
5. **把 Atomics 112 个 harness 失败当作一个 agent-report 协议问题处理**。

---

# 十三、最终判断

从 76.54% 到 83.60% 的提升证明，项目仍有明显的成片增益空间；但 83.60% 到 95% 的难度会高得多，因为低成本缺失功能已经大量补齐，剩余项更多涉及：

- 共享抽象操作；
- 完整求值顺序；
- abrupt completion；
- Realm 与 internal slots；
- async job；
- module linking；
- Temporal/Intl 大型算法；
- ECMAScript RegExp 后端。

95% 不是简单的下一次小版本目标，而应作为一个以“统一语义内核”为中心的大版本目标。

最重要的组织原则是：

> **一个完整功能只有一个 owner；共享底层只有一个实现；其他组通过冻结接口使用；任何跨层修改都由该功能 owner 对最终 Test262 结果负责。**
